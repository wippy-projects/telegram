version: "1.0"
namespace: telegram.commands

entries:
  # ── /help ──────────────────────────────────────────────
  - name: help
    kind: registry.entry
    meta:
      type: telegram.command
      command: /help
      description: "Show available commands"
      handler: telegram.commands:help_handler

  - name: help_handler
    kind: function.lua
    source: file://help.lua
    method: handler
    modules:
      - logger
      - funcs
      - registry
